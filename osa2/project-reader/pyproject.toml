[tool.poetry]
name = "project-reader"
version = "0.1.0"
description = "Testausta"
authors = ["Oliver Kandén"]

[tool.poetry.dependencies]
python = "^3.10"
toml = "^0.10.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
